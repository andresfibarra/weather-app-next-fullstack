'use client'

import React from 'react'; 
import Logo from './Logo'

function NavBar() {
  return (
    <header>
      <div>WeatherApp</div>

      <nav>
        <Logo />
      </nav>
    </header>
  )
}